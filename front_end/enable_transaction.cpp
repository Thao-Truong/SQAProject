#include <iostream>
#include <string>
#include <stdio.h>
#include <stdlib.h>

#include "users.h"
#include "enable_transaction.h"

using namespace std;

// Enables a disabled account and writes the transaction line
string EnableAccount(Users user) {
  // To do: Ask for bank account holder's name

  // To do: Ask for account number

  // To do: Change bank account from disabled(D) to active(A)

  // To do: Write and return transaction line
}       