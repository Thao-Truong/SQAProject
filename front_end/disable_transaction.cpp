#include <iostream>
#include <string>
#include <stdio.h>
#include <stdlib.h>

#include "users.h"
#include "disable_transaction.h"

using namespace std;

// Disables an enable account and writes the transaction line
string DisableAccount(Users user) {
  // To do: Ask for bank account holder's name

  // To do: Ask for account number

  // To do: Change bank account from active(A) to disabled(D)

  // To do: Write and return transaction line
}       